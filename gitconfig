# [user] and [filter "lfs"] were redacted
# save in ~/.gitconfig

################ everything below is for git aliasing ################\
[alias]
        co = checkout
        br = branch
        ci = commit
        st = status
        rb = rebase
